#include<stdio.h>
int main()
{
int n,count=0,i,r,c=0,counts=0;
   for(n=1;n<=100;n++)
   {
       count=0;
       for(i=2;i<=n/2;i++)
       {
           r=n%i;
           if(r==0)
           {
               count++;
               counts=c+count;
               printf("%d",counts);
               break;
           }
          // printf("%d",count);
       }
       //if(count==0)
       //printf("\n%d",n);
       //printf("%d",count);
   }
